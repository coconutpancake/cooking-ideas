EXPO_PUBLIC_API_BASE_URL=https://api.potinspire.cn

# Must match apps/web-api API_CLIENT_TOKEN while the MVP API gate is enabled.
# This is a temporary app-level gate, not a final user identity token.
EXPO_PUBLIC_API_AUTH_TOKEN=replace_with_the_same_long_random_token
